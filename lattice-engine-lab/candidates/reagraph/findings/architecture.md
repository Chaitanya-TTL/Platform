# Architecture Boundary

Reagraph owns WebGL rendering, picking, camera interaction, clustering visualization, native edge aggregation, labels, and spatial layout. Lattice-owned projection state determines datasets, search, neighborhoods, paths, evidence classification, semantic summaries, and safe stress loading. Visual clusters and node importance are analytical aids and never source-system truth.
