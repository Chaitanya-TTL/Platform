# Cytoscape security findings

Only simulated fixture data is rendered. Labels are passed through Cytoscape data fields, not injected as HTML. Vite is pinned to 7.3.6 with zero known install-time vulnerabilities. No production payload, credential, connector, environment file, Graphin package, or commercial dependency is used.
