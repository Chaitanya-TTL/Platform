import{Component,useState,type ErrorInfo,type ReactNode}from"react";import{CytoscapeExperiment}from"./CytoscapeExperiment";
class Boundary extends Component<{children:ReactNode;resetKey:number},{error:Error|null}>{state:{error:Error|null}={error:null};static getDerivedStateFromError(error:Error){return{error}}componentDidCatch(error:Error,info:ErrorInfo){console.error("Cytoscape experiment failed",error,info)}componentDidUpdate(previous:Readonly<{children:ReactNode;resetKey:number}>){if(previous.resetKey!==this.props.resetKey&&this.state.error)this.setState({error:null})}render(){return this.state.error?<main className="fatal"><h1>Renderer failed</h1><p>{this.state.error.message}</p><button onClick={()=>location.reload()}>Reload</button></main>:this.props.children}}
export function App(){const[mounted,setMounted]=useState(true);const[key,setKey]=useState(0);const remount=()=>{setMounted(false);requestAnimationFrame(()=>{setKey(value=>value+1);setMounted(true)})};return <Boundary resetKey={key}><div className="lab"><nav><span>Strict-mode lifecycle control</span><button onClick={()=>setMounted(value=>!value)}>{mounted?"Destroy renderer":"Mount renderer"}</button><button onClick={remount}>Clean remount</button></nav>{mounted?<CytoscapeExperiment key={key}/>:<main className="destroyed"><h1>Renderer destroyed</h1><p>The Cytoscape instance, listeners, and canvas are unmounted.</p><button onClick={()=>setMounted(true)}>Mount renderer</button></main>}</div></Boundary>}
