# G6 architecture findings

Canonical entities, relationships, evidence classes, filters, neighborhood expansion, path calculation, focus, and measurements stay outside G6. G6 owns canvas rendering, combos, element states, behaviors, layout transitions, viewport operations, and lifecycle. Domain combos are presentation groups, not business entities.
