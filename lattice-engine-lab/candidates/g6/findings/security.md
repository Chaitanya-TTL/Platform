# G6 security findings

Only simulated fixture data is used. No HTML is injected into graph labels. Vite is pinned to 7.3.6 with zero known install-time vulnerabilities. No production credentials, payloads, connectors, Graphin package, or commercial package is present.
