# G6 runtime evaluation

The browser records first render, selection, incremental expansion, path highlight, path isolation, layout switching, focus-fit, and camera restoration. Diagnostics expose renderer instances, explicit listeners, and node-overlap pairs where G6 render-bound APIs are available. Destroy and remount call `off()` and `destroy()`.
