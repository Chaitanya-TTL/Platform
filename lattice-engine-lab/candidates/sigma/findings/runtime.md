# Runtime evaluation

The browser exposes graph construction, Graphology ingestion, metric computation, community detection, ForceAtlas2 initialization and stabilization, Sigma initialization, first meaningful render, dataset switching, filtering, search focus, neighborhood isolation, shortest path, path highlight, community isolation, camera restoration, and renderer destruction timings. Renderer, listener, and worker counts remain visible.
