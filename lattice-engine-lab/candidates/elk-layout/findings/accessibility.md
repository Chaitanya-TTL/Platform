# Accessibility findings

The layout service itself is nonvisual. The neutral renderer exposes an SVG role and a JSON semantic representation of nodes, edges, ports, routing, overflow, and stability. Production accessibility remains the responsibility of the Lattice React shell.
