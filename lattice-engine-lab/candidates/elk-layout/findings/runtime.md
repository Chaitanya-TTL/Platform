# Runtime findings

Each layout is run three times; median duration and coordinate determinism are reported. Result application is measured separately. Existing-node displacement, pillar displacement, focus displacement, overlaps, and approximate segment crossings are calculated from normalized output.
