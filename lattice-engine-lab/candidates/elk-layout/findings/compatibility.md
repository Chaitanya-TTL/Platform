# Compatibility findings

The experiment uses ELK.js 0.12.0 through the bundled module in Vite. The neutral SVG renderer exists only to inspect layout output. Production React Flow consumption would map node bounds to React Flow positions and edge sections to custom routed edges.
