# ELK architecture findings

ELK owns only positions, dimensions, ports, bend points, edge sections, spacing, ordering, and stability metadata. Scenario semantics and visibility budgets remain outside ELK. The layout-result contract is intentionally narrow and renderer-consumable by a future React Flow hybrid.
