# React Flow runtime evaluation

Instrumentation records first render, selection, expansion, collapse, focus-fit, path highlight, and camera restoration in the browser. The lifecycle toolbar supports destroy, mount, and clean remount under React Strict Mode. Listener balance and React Flow canvas count are shown as runtime diagnostics. Manual browser acceptance is required because heap and GPU-memory behavior cannot be proven by a production build alone.
