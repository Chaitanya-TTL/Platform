# React Flow security findings

The spike uses simulated data only. It contains no connector calls, credentials, environment files, or production payloads. Vite is pinned to 7.3.6 after the earlier 7.1.3 audit findings. Renderer content is displayed as React text rather than raw HTML. Final audit and lockfile evidence remain installation-generated and are not packaged here.
