# React Flow accessibility findings

The canvas is supplemented by a keyboard-operable semantic entity and relationship representation. Filter controls use native checkboxes, the inspector uses an ARIA live region, and visible controls remain functional with reduced motion. Graph geometry itself is not a complete accessible representation; the semantic panel remains mandatory for production consideration.
