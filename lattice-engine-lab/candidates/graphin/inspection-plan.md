# Inspection plan

Use only a disposable, credential-free environment. Record registry metadata, publication history, advisories, lifecycle scripts, tarball contents, registry integrity, local SHA-512, license, peers, engines, and the complete transitive dependency tree before any runtime test.
