import fs from"node:fs";import path from"node:path";const lab=path.resolve(import.meta.dirname,"..");for(const n of fs.readdirSync(path.join(lab,"candidates"))){if(n==="graphin")continue;const p=path.join(lab,"candidates",n,"package.json");if(fs.existsSync(p)&&!fs.existsSync(path.join(path.dirname(p),"package-lock.json"))){console.error(`MISSING LOCKFILE ${n}`);process.exitCode=1}}
