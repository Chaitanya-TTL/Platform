import type{CanonicalGraphFixture,EntityId}from"../contracts/canonical-graph";export function visibleNeighborhood(g:CanonicalGraphFixture,id:EntityId,hops=1){const ids=new Set<EntityId>([id]);for(let h=0;h<hops;h++)for(const x of g.relationships)if(ids.has(x.sourceId)||ids.has(x.targetId)){ids.add(x.sourceId);ids.add(x.targetId)}return{entities:g.entities.filter(x=>ids.has(x.id)),relationships:g.relationships.filter(x=>ids.has(x.sourceId)&&ids.has(x.targetId))}}
