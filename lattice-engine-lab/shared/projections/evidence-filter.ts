import type{CanonicalGraphFixture,EvidenceClass}from"../contracts/canonical-graph";export function filterByEvidence(g:CanonicalGraphFixture,c:EvidenceClass[]){const entities=g.entities.filter(x=>c.includes(x.evidenceClass)),ids=new Set(entities.map(x=>x.id));return{entities,relationships:g.relationships.filter(x=>c.includes(x.evidenceClass)&&ids.has(x.sourceId)&&ids.has(x.targetId))}}
