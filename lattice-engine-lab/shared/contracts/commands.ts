import type{EntityId,RelationshipId}from"./canonical-graph";import type{VisibleProjection}from"./projection";export interface CameraSnapshot{centerX:number;centerY:number;zoom:number;rendererPayload?:unknown}export type EvaluationCommand={type:"select";entityId:EntityId}|{type:"focus";entityId:EntityId}|{type:"expand";entityId:EntityId}|{type:"collapse";entityId:EntityId}|{type:"highlight-path";relationshipIds:RelationshipId[]}|{type:"apply-projection";projection:VisibleProjection}|{type:"restore-camera";camera:CameraSnapshot}|{type:"fit-view"}|{type:"destroy"};
