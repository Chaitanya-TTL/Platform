import { params, result } from "@/lib/item-explorer/server";
export const dynamic = "force-dynamic";
type R=Record<string,unknown>;const rec=(v:unknown):v is R=>Boolean(v)&&typeof v==="object"&&!Array.isArray(v);const str=(v:unknown)=>typeof v==="string"?v.trim():v==null?"":String(v);
export async function GET(request:Request){const {query}=params(request);const started=Date.now();if(!query)return Response.json(result("configit","failed",0,{error:"Enter a Configit package or work-item name."}),{status:400});try{const response=await fetch(`${new URL(request.url).origin}/api/bom-configit?query=${encodeURIComponent(query)}`,{cache:"no-store"});const payload=await response.json() as R;if(response.status===404)return Response.json(result("configit","empty",Date.now()-started));if(!response.ok)throw new Error(str(payload.error)||"Configit discovery failed.");const authoring=rec(payload.authoringWorkItem)?payload.authoringWorkItem:{};const published=rec(payload.publishedPackage)?payload.publishedPackage:{};const workResolution=rec(authoring.workItemResolution)?authoring.workItemResolution:{};const model=rec(authoring.productModel)?authoring.productModel:{};const packageResolution=rec(published.packageResolution)?published.packageResolution:{};const productResolution=rec(published.productResolution)?published.productResolution:{};const authoringAvailable=authoring.available===true,publishedAvailable=published.available===true;const nativeId=authoringAvailable?str(authoring.workItemId):str(published.productId)||str(productResolution.selectedProductId);const name=str(workResolution.resolvedWorkItemName)||str(model.description)||str(productResolution.selectedProductName)||query;const context={objectType:authoringAvailable?"configit-work-item":"configit-package",lifecycleState:str(workResolution.status)||str(packageResolution.state)||undefined,version:str(packageResolution.versionId)||undefined,query,workItemId:str(authoring.workItemId)||undefined,workItemName:str(workResolution.resolvedWorkItemName)||undefined,productModelCode:str(model.code)||undefined,productModelDescription:str(model.description)||undefined,logicalPackagePath:str(packageResolution.logicalPackagePath)||undefined,resolvedPackagePath:str(packageResolution.resolvedPackagePath)||undefined,packagePath:str(published.packagePath)||undefined,packageVersionId:str(packageResolution.versionId)||undefined,packageState:str(packageResolution.state)||undefined,publishedAt:str(packageResolution.publishedAt)||undefined,authoringAvailable:String(authoringAvailable),publishedAvailable:String(publishedAvailable)};return Response.json(result("configit","complete",Date.now()-started,{candidates:[{candidateId:`configit:${nativeId||query}`,source:"configit",nativeId:nativeId||query,name,description:str(workResolution.description)||str(model.description)||undefined,matchType:authoringAvailable?"exact-name":"source-result",matchReasons:[authoringAvailable?"Exact Configit work-item name match.":"Published Configit package match."],context,retrievedAt:str(payload.extractedAt)||new Date().toISOString(),providerMode:"live"}]}));}catch(error){return Response.json(result("configit","failed",Date.now()-started,{error:error instanceof Error?error.message:"Configit discovery failed."}),{status:502})}}
