import { NextResponse } from "next/server";
import type { LatticeHandoff } from "@/features/lattice-next/contracts/handoff";
import type { EngineeringIntelligenceInvestigationV1,IntelligenceEntity,IntelligenceProperty,IntelligenceRelationship } from "@/features/lattice-next/contracts/intelligence-v1";
type R=Record<string,unknown>;const rec=(v:unknown):v is R=>Boolean(v)&&typeof v==="object"&&!Array.isArray(v);const arr=(v:unknown):R[]=>Array.isArray(v)?v.filter(rec):[];const str=(v:unknown)=>typeof v==="string"?v.trim():v==null?"":String(v);const safe=(v:unknown)=>str(v).toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"")||"unknown";const prop=(v:unknown):IntelligenceProperty=>typeof v==="boolean"?{type:"boolean",boolean:v}:typeof v==="number"?{type:"number",number:v}:{type:"text",text:str(v)};
export async function POST(request:Request){try{const {handoff}=await request.json() as {handoff?:LatticeHandoff};const source=handoff?.sources.find(x=>x.source==="configit");if(!handoff||!source)throw new Error("Configit investigation handoff is required.");const payload=source.intelligencePayload;if(!rec(payload))throw new Error("Configit intelligence payload is unavailable.");const captured=str(payload.extractedAt)||source.capturedAt||handoff.createdAt,assembled=new Date().toISOString(),temporal={capturedAt:captured,assembledAt:assembled,freshness:"current" as const};const provenance=(nativeId?:string)=>({source:"configit" as const,provider:"configit-authoritative-extractor",nativeId,observedAt:captured,authority:"source-authoritative" as const});const entities:IntelligenceEntity[]=[];const relationships:IntelligenceRelationship[]=[];const subjectId=`configit:subject:${safe(source.nativeId||handoff.subjectLabel)}`;const add=(kind:string,id:string,name:string,properties:R={},nativeId?:string)=>{if(!entities.some(x=>x.id===id))entities.push({id,kind,displayName:name,identityQuality:"native",assertion:"fact",properties:Object.fromEntries(Object.entries(properties).filter(([,v])=>v!==undefined&&v!==null&&v!=="").map(([k,v])=>[k,prop(v)])),provenance:provenance(nativeId||id),temporal});return id};const link=(kind:string,family:string,from:string,to:string)=>relationships.push({id:`configit:rel:${safe(kind)}:${safe(from)}:${safe(to)}`,kind,family,sourceEntityId:from,targetEntityId:to,assertion:"fact",confidence:"verified",evidenceIds:[],properties:{},provenance:provenance(`${from}:${to}`),temporal});const field=(label:string,value:unknown,key=label)=>{if(value===undefined||value===null||value==="")return;const id=add("product-detail",`configit:detail:${safe(key)}`,label,{value,attributeKey:key},key);link("has-product-context","product-detail",subjectId,id)};const authoring=rec(payload.authoringWorkItem)?payload.authoringWorkItem:{},published=rec(payload.publishedPackage)?payload.publishedPackage:{},resolution=rec(authoring.workItemResolution)?authoring.workItemResolution:{},model=rec(authoring.productModel)?authoring.productModel:{};field("Product model code",model.code);field("Product model description",model.description);field("Brand code",model.brandCode);field("Arithmetic rules",model.useArithmeticRules);field("Work-item ID",authoring.workItemId);field("Work-item name",resolution.resolvedWorkItemName);field("Work-item status",resolution.status);const packageResolution=rec(published.packageResolution)?published.packageResolution:{};field("Package product ID",published.productId);field("Logical package path",packageResolution.logicalPackagePath);field("Resolved package path",packageResolution.resolvedPackagePath);field("Package version",packageResolution.versionId);field("Package state",packageResolution.state);field("Published time",packageResolution.publishedAt);
if(published.available===true){const visit=(node:R,path:string,parent?:string)=>{const native=str(node.id)||path,id=add("part-occurrence",`configit:occurrence:${safe(native)}:${safe(path)}`,str(node.name)||native,{quantity:node.quantity},native);link(parent?"contains":"represented-by","structure",parent||subjectId,id);for(const [index,child] of arr(node.children).entries())visit(child,`${path}.${index}`,id)};for(const [index,node] of arr(published.bom).entries())visit(node,String(index))}
const configuration=rec(authoring.configuration)?authoring.configuration:{};for(const family of arr(configuration.families)){const code=str(family.code),fid=add("configuration-family",`configit:family:${safe(code)}`,str(family.name)||code,{code,familyType:family.familyType,workItemId:authoring.workItemId},code);link("has-configuration-family","configuration",subjectId,fid);for(const feature of arr(family.features)){const fcode=str(feature.code),id=add("configuration-feature",`${fid}:feature:${safe(fcode)}`,str(feature.name)||fcode,{code:fcode,parentFamily:code,workItemId:authoring.workItemId},fcode);link("contains-feature","configuration",fid,id)}}
for(const rule of arr(configuration.rules)){const code=str(rule.code),rid=add("configuration-rule",`configit:rule:${safe(code)}`,str(rule.description)||code,{code,expression:rule.expression,enabled:rule.enabled,locked:rule.locked,workItemId:authoring.workItemId},code);link("governed-by","rules",subjectId,rid);const effect=rec(rule.effectivity)?rule.effectivity:{};for(const [label,value] of [["Expression",rule.expression],["Enabled",rule.enabled],["Locked",rule.locked],["Start event",effect.startEventCode],["End event",effect.endEventCode]])if(value!==undefined&&value!==null&&value!==""){const id=add("configuration-rule-field",`${rid}:field:${safe(label)}`,label,{value},`${code}:${label}`);link("contains","rules",rid,id)}}
const localization=rec(authoring.localization)?authoring.localization:{};const meaningful=(value:unknown)=>{const text=str(value);return text&&text!=="[object Object]"?text:""};const nestedText=(value:unknown,...keys:string[])=>{if(typeof value==="string"||typeof value==="number")return meaningful(value);if(rec(value))for(const key of keys){const text=nestedText(value[key],"code","name","description","displayName","value","label");if(text)return text}return""};const languages=arr(localization.languages),languageIds=new Map<string,string>();for(const language of languages){const code=nestedText(language.code??language.languageCode,"code","value","label"),name=nestedText(language.description??language.name??language.displayName,"name","description","displayName","value","label")||code;if(!name)continue;const key=code||name,id=add("localization-language",`configit:language:${safe(key)}`,name,{code:code||undefined,workItemId:authoring.workItemId},key);languageIds.set(key,id);if(code)languageIds.set(code,id);link("has-language","localization",subjectId,id)}for(const [kind,items] of [["family",arr(localization.familyTranslations)],["feature",arr(localization.featureTranslations)],["section",arr(localization.sectionTranslations)]] as const)for(const t of items){const code=meaningful(t.code),lang=nestedText(t.languageCode,"code","value","label"),sourceText=meaningful(t.sourceText),translatedText=meaningful(t.translatedText);if(!lang||(!sourceText&&!translatedText&&!code))continue;const parent=languageIds.get(lang)||add("localization-language",`configit:language:${safe(lang)}`,lang,{code:lang},lang);if(!languageIds.has(lang)){languageIds.set(lang,parent);link("has-language","localization",subjectId,parent)}const id=add("localization-translation",`${parent}:${kind}:${safe(code||sourceText)}:${safe(t.declarationCode)}`,sourceText||code||translatedText,{sourceText:sourceText||undefined,translatedText:translatedText||undefined,languageCode:lang,declarationCode:meaningful(t.declarationCode)||undefined,translationKind:kind,referencedCode:code||undefined},code||sourceText);link("contains-translation","localization",parent,id)}
if(authoring.available===true){const evidenceGroup=add("source-evidence-group","configit:evidence:authoring","Authoring Work Item",{workItemId:authoring.workItemId,matchMethod:resolution.method,scannedWorkItems:resolution.scannedWorkItems,status:resolution.status},str(authoring.workItemId));link("supported-by","source-evidence",subjectId,evidenceGroup)}if(published.available===true){const packageGroup=add("source-evidence-group","configit:evidence:package","Published Package",{logicalPackagePath:packageResolution.logicalPackagePath,resolvedPackagePath:packageResolution.resolvedPackagePath,versionId:packageResolution.versionId,state:packageResolution.state,publishedAt:packageResolution.publishedAt},str(published.productId));link("supported-by","source-evidence",subjectId,packageGroup)}
const status=payload.status==="partial_success"?"partial":"available";const result:EngineeringIntelligenceInvestigationV1={contractVersion:"1.0",investigationId:`configit-${handoff.handoffId}`,subject:{id:subjectId,displayName:handoff.subjectLabel,preferredProductId:source.nativeId,candidateIds:[source.nativeId||str(authoring.workItemId)].filter(Boolean),confidence:"verified",reasons:["Selected authoritative Configit work item or package"],provenance:provenance(source.nativeId),temporal},entities,relationships,evidence:[],findings:[],sources:[{entityId:subjectId,source:"configit",status,provenance:provenance(source.nativeId)}],identityClusters:[],temporal:{requestedAt:handoff.createdAt,...temporal},summary:{entityCount:entities.length,relationshipCount:relationships.length,evidenceCount:0,findingCount:0,availableSourceCount:status==="available"?1:0,partialSourceCount:status==="partial"?1:0,status:status==="available"?"success":"partial-success"}};return NextResponse.json(result)}catch(error){return NextResponse.json({message:error instanceof Error?error.message:"Configit intelligence failed."},{status:500})}}
