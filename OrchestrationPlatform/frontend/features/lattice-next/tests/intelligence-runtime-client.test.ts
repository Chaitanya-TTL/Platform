import{describe,expect,it,vi}from"vitest";import{assembleCanonicalInvestigation}from"../infrastructure/intelligence-api-client";import{legacyHandoffToIntelligence}from"../engines/legacy-handoff-to-intelligence";import{smallFixture}from"./fixtures";describe("runtime client",()=>{it("validates canonical response",async()=>{const v=legacyHandoffToIntelligence(smallFixture);vi.stubGlobal("fetch",vi.fn(async()=>new Response(JSON.stringify(v),{status:200})));await expect(assembleCanonicalInvestigation(v)).resolves.toEqual(v);vi.unstubAllGlobals()})});