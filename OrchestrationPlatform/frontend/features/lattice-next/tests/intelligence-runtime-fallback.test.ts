import{describe,expect,it,vi}from"vitest";import{resolveIntelligenceRuntime}from"../infrastructure/intelligence-runtime";import{smallFixture}from"./fixtures";describe("runtime fallback",()=>{it("labels backend outage fallback",async()=>{vi.stubGlobal("fetch",vi.fn(async()=>new Response(JSON.stringify({message:"offline"}),{status:503})));const v=await resolveIntelligenceRuntime(smallFixture);expect(v.mode).toBe("legacy-compatibility");expect(v.warning).toContain("clearly labelled");vi.unstubAllGlobals()})});