import{describe,expect,it}from"vitest";import{orderedRequirementRevisions,requirementAssociation}from"../requirements/requirement-presentation";describe("requirements projection",()=>{it("orders actual temporal metadata",()=>{const v=orderedRequirementRevisions([{id:"a",canonicalKind:"requirement-revision",attributes:{effectiveAt:"2024-01-01"}},{id:"b",canonicalKind:"requirement-revision",attributes:{effectiveAt:"2025-01-01"}}] as never);expect(v.map(x=>x.id)).toEqual(["b","a"])});it("distinguishes inferred traces",()=>expect(requirementAssociation({confidence:.72} as never).label).toBe("Inferred trace"))});