import{afterEach,describe,expect,it,vi}from"vitest";import{investigateWindchill}from"../infrastructure/windchill-intelligence-client";afterEach(()=>vi.unstubAllGlobals());describe("Windchill investigation client",()=>{it("uses the existing Windchill intelligence middleware",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,json:async()=>({contractVersion:"1.0",investigationId:"wc",entities:[],relationships:[],evidence:[],findings:[],sources:[],identityClusters:[]})}));await investigateWindchill({}as never);expect(fetch).toHaveBeenCalledWith("/api/engineering/intelligence/windchill",expect.objectContaining({method:"POST"}))})});
