"use client";
import type { ReactNode } from "react";
import { IconDatabase, IconHierarchy, IconLink } from "@tabler/icons-react";
import type { EngineeringEntity, EngineeringRelationship } from "../domain/model";
export function EntityInspector({ entity, relationships }: { entity: EngineeringEntity | null; relationships: EngineeringRelationship[] }) { if (!entity) return <aside className="flex h-full items-center justify-center border-l border-slate-800 p-6 text-sm text-slate-500">Select an engineering entity.</aside>; const attributes=Object.entries(entity.attributes); return <aside className="h-full overflow-auto border-l border-slate-800 bg-slate-950/75 p-5 text-slate-200"><p className="text-[10px] font-bold uppercase tracking-[.16em] text-cyan-400">{entity.provenance.sourceLabel} evidence</p><h2 className="mt-2 text-lg font-semibold">{entity.name}</h2><p className="mt-1 font-mono text-[10px] text-slate-500">{entity.sourceNodeId}</p><section className="mt-6"><Heading icon={<IconHierarchy/>} text="Engineering context"/><dl className="mt-3 grid grid-cols-2 gap-2 text-xs"><Metric label="Type" value={entity.kind}/><Metric label="Level" value={entity.level + 1}/><Metric label="Links" value={relationships.length}/><Metric label="Source" value={entity.source}/></dl></section><section className="mt-6"><Heading icon={<IconDatabase/>} text="Native attributes"/><div className="mt-2">{attributes.length ? attributes.map(([key,value]) => <div key={key} className="flex justify-between gap-4 border-b border-slate-800 py-2 text-xs"><dt className="text-slate-500">{key}</dt><dd className="max-w-[60%] break-words text-right">{String(value)}</dd></div>) : <p className="text-xs text-slate-500">No native attributes were supplied.</p>}</div></section><section className="mt-6"><Heading icon={<IconLink/>} text="Provenance"/><p className="mt-2 text-xs leading-5 text-slate-400">Captured from {entity.provenance.sourceLabel}{entity.provenance.nativeId ? ` as ${entity.provenance.nativeId}` : ""}. The source structure remains unchanged.</p></section></aside>; }
function Heading({icon,text}:{icon:ReactNode;text:string}){return <h3 className="flex items-center gap-2 text-[10px] font-bold uppercase tracking-[.14em] text-slate-500"><span className="[&_svg]:h-4 [&_svg]:w-4">{icon}</span>{text}</h3>}
function Metric({label,value}:{label:string;value:string|number}){return <div className="rounded-lg border border-slate-800 bg-slate-900/55 p-3"><dt className="text-[9px] uppercase text-slate-600">{label}</dt><dd className="mt-1 truncate font-semibold text-slate-200">{value}</dd></div>}
