import type{EngineeringEntity,InvestigationGraph}from"../domain/model";import{orderedRequirementRevisions,requirementAuthority}from"../requirements/requirement-presentation";export function RequirementInspectorSections({graph,entity}:{graph:InvestigationGraph;entity:EngineeringEntity}){const ids=new Set(graph.relationships.flatMap(r=>r.from===entity.id?[r.to]:r.to===entity.id?[r.from]:[]));const revisions=orderedRequirementRevisions(graph.entities.filter(e=>ids.has(e.id)));return <section className="mt-6 border-t border-slate-800 pt-5 text-xs"><h3 className="font-bold uppercase tracking-[.14em] text-cyan-400">Requirement intelligence</h3><p className="mt-2">Authority: {requirementAuthority(entity)}</p><p>Status: {String(entity.attributes.status??"Not supplied")}</p>{revisions.map((x,i)=><p key={x.id} className={i?"text-slate-500":"text-emerald-300"}>{x.name}{i?" · Historical":" · Current"}</p>)}<p className="mt-2 text-slate-500">Use the existing Requirement Explorer or timeline for deep detail.</p></section>}