import type{EngineeringEntity,EngineeringRelationship}from"../domain/model";export const isRequirementEntity=(e:EngineeringEntity)=>["requirement","requirement-revision","document"].includes(e.canonicalKind??e.kind);export const requirementAuthority=(e:EngineeringEntity)=>String(e.attributes.authority??e.attributes.Authority??e.provenance.sourceLabel);export const requirementAssociation=(r:EngineeringRelationship)=>({exact:r.verified===true||r.confidence===1,label:r.verified?"Exact trace":"Inferred trace",confidence:r.confidence});export const orderedRequirementRevisions=(e:EngineeringEntity[])=>[...e].filter(x=>x.canonicalKind==="requirement-revision").sort((a,b)=>String(b.attributes.effectiveAt??b.temporal?.capturedAt??"").localeCompare(String(a.attributes.effectiveAt??a.temporal?.capturedAt??""))||a.id.localeCompare(b.id));